# Future Work

- **Schedule adherence %** — compare scheduled time (from timeline `scheduledStartTime` + `estimatedMinutes`) vs actual tracked time (work sessions) to calculate how well the user followed their planned schedule
- **Preferred time for habits** — allow users to set a default time on habits (e.g., "Meditate at 7am"). `ensureHabitSubtasksForDate` auto-sets `scheduledStartTime` so habits appear on the timeline without manual drag
